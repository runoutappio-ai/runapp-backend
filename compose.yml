services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: runout
      POSTGRES_USER: runout
      POSTGRES_PASSWORD: runout
    ports: ["5432:5432"]
    # PostgreSQL 18 uses a major-version-specific data directory.
    volumes: ["runout-postgres:/var/lib/postgresql"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U runout"]
      interval: 5s
      timeout: 3s
      retries: 10
  keycloak:
    image: quay.io/keycloak/keycloak:26.7.3
    command: start-dev --import-realm
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USERNAME:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:-admin}
      KEYCLOAK_REGISTRATION_CLIENT_SECRET: ${KEYCLOAK_REGISTRATION_CLIENT_SECRET:-runout-registration-secret-change-me}
      KEYCLOAK_AUTHENTICATION_CLIENT_SECRET: ${KEYCLOAK_AUTHENTICATION_CLIENT_SECRET:-runout-auth-secret-change-me}
      KEYCLOAK_GOOGLE_CLIENT_ID: ${KEYCLOAK_GOOGLE_CLIENT_ID:-}
      KEYCLOAK_GOOGLE_CLIENT_SECRET: ${KEYCLOAK_GOOGLE_CLIENT_SECRET:-}
      KC_HOSTNAME_URL: ${KEYCLOAK_PUBLIC_URL:-http://localhost:8081}
      KC_HOSTNAME_ADMIN_URL: http://localhost:8081
      # Enable only when using a HTTPS reverse proxy/tunnel such as Cloudflare.
      # In local HTTP mode this makes Keycloak treat requests as external and can trigger "HTTPS required".
      # KC_PROXY_HEADERS: xforwarded
    ports:
      - "8081:8080"
    volumes:
      - keycloak-data:/opt/keycloak/data
      - ./keycloak/realm:/opt/keycloak/data/import:ro
volumes:
  runout-postgres:
  keycloak-data:
